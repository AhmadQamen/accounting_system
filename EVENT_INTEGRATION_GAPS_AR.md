# تقرير نواقص ربط الأحداث — EVENT_CATALOG_AR.md v1

## عمليات محلية لا يعرّف لها العقد Event

- إنشاء وإغلاق السنة المالية وتغيير السنة النشطة: لا يوجد `financial_year` في مصفوفة v1، لذلك تبقى محلية ولا تدخل outbox.
- مواصفات المنتج: لا يوجد `product_specification` في المصفوفة، لذلك تبقى محلية.
- إنشاء `inventory_item` المساعد: هو projection داخلي وليس aggregate معتمدًا، لذلك لا يرسل.
- فتح وإغلاق `cash_sessions`: حُفظت محليًا فقط، ولا يرسل أي `cash_session`.
- تعديل اسم صندوق موجود: الكتالوج يعرّف `CashboxCreated` فقط ولا يعرّف `CashboxUpdated`.
- أرشفة مستودع: الكتالوج لا يعرّف `WarehouseDeleted`.
- سعر البيع `sale_price_minor` في وحدة المنتج: موجود محليًا، لكن payload الخاص بـ`ProductUnitCreated/Updated` لا يحتوي حقل سعر في العقد، لذلك لا يرسل ضمن الحدث.

## أحداث في العقد لا تقابلها عملية حالية في المستودعات

- `CategoryDeleted`.
- `ProductUpdated` و`ProductDeleted`.
- `ProductUnitDeleted`.
- `BarcodeDeleted`.
- `ExpenseVoided` و`PartyPaymentVoided`؛ لا توجد حاليًا عمليات إلغاء مقابلة في `cash_repository.dart`.

## قيود واجهة الإلغاء الحالية

- `document_repository.dart` يرسل `SaleVoided` و`PurchaseVoided` فقط.
- إلغاء المرتجعات أو التالف غير مسموح عبر محرك الأحداث لأن مصفوفة v1 لا تحتوي أنواعًا مقابلة.
- سبب الإلغاء مدعوم كوسيط اختياري للمحافظة على توافق استدعاءات الواجهة القديمة؛ القيمة الافتراضية الحالية «إلغاء المستند». يفضّل جعل إدخال السبب إلزاميًا في الواجهة قبل الإنتاج.

## ملاحظة دفتر الطرف في المستند

شكل v1 يوفر `partyLedgerEntryId` مفردًا، بينما بعض المستندات المحلية تنشئ قيد استحقاق وقيد دفع منفصلين. يرسل التطبيق أول قيد في الحقل المتاح ولا يخترع مصفوفة غير موجودة في العقد. يحتاج العقد إلى توضيح/توسعة إذا كان الجهاز الآخر مطالبًا بإعادة بناء القيدين من معرفين مستقلين.
