# التقرير النهائي لربط Flutter مع Backend v1

تاريخ التحقق: 2026-09-25

## ما يعمل فعليًا

- عنوان API مركزي عبر `ApiConfig` وقيمته الإنتاجية الافتراضية:
  `https://accounting.alkhaleel-mosque.com/api/v1`.
- جميع استدعاءات Flutter تمر عبر Dio المركزي ومسارات نسبية؛ يمنع الإعداد HTTP البعيد، وعناوين IP، والمنافذ المخصصة، و`/admin/`، وتكرار `/api/v1`.
- المصادقة مربوطة بـ login وrefresh وlogout و`GET /me`، والتوكنات في `flutter_secure_storage`، مع refresh بنمط single-flight وإعادة طلب واحدة فقط.
- اختيار المؤسسة صريح عند تعدد memberships، ويمكن العودة لاختيار مؤسسة أخرى من الإعدادات أو من شاشة الجهاز الملغى.
- تسجيل الجهاز مستقل لكل مؤسسة، والمزامنة تتوقف عند `revoked=true`.
- واجهة المزامنة تعرض آخر مزامنة، المعلّق، التعارضات، المرفوض، خطأ آخر محاولة، والجهاز الملغى.
- لا تظهر رسالة نجاح إلا بعد إعادة قراءة الحالة والتأكد من عدم وجود معلّق أو تعارض أو مرفوض أو خطأ أو إلغاء للجهاز.
- محرك المزامنة يستخدم bootstrap/push/pull/ack/status، ويطبق الأحداث بحسب `eventType` و`payload` مع منع التكرار ومعاملات SQLite.

## إعدادات المنصات

- Android Manifest الرئيسي يحتوي `android.permission.INTERNET`.
- لا يوجد `usesCleartextTraffic=true` في إعداد الإنتاج.
- لا يوجد ATS exception في iOS.
- ظهور `http://` داخل ملفات XML هو namespace/DOCTYPE قياسي، وليس عنوان طلب للتطبيق.

## مراجعة العناصر المحظورة

- لا يوجد IP خادم قديم أو المنفذ `18081` في كود التطبيق.
- لا توجد طلبات إلى `/admin/`؛ الإشارة الوحيدة له حارس يرفض هذا المسار.
- لا يوجد public registration؛ كلمة register الموجودة تخص تسجيل الجهاز فقط.
- Draft محلي ولا يدخل outbox؛ الحارس في `OutboxService` يعيد دون إنشاء حدث عند `action == draft`.
- `cash_sessions` ميزة SQLite محلية ولا تستخدم ضمن sync أو Event Catalog.

## ما بقي خارج عقد v1

- السنوات المالية ومواصفات المنتج لا تملك Events في v1 وتبقى محلية.
- فتح وإغلاق جلسات الصندوق محلي فقط.
- تعديل اسم صندوق، أرشفة مستودع، وسعر البيع داخل وحدة المنتج لا تملك payload/event مطابقًا في v1.
- لا توجد بعد عمليات مستودع محلية تقابل بعض أحداث الحذف/التعديل المعرفة في الكتالوج، كما هو مفصل في `EVENT_INTEGRATION_GAPS_AR.md`.
- عقد v1 يعطي `partyLedgerEntryId` واحدًا للمستند، بينما بعض الحالات المحلية تحتاج قيدي استحقاق ودفع؛ يلزم توسيع العقد إذا كان مطلوبًا نقل معرفي القيدين صراحة.

## نتائج التحقق

- `GET https://accounting.alkhaleel-mosque.com/api/v1/me` دون token أعاد HTTP `401` كما هو متوقع؛ لم تُستخدم أي بيانات دخول.
- `flutter test`: نجح، 28 من 28 اختبارًا.
- `dart analyze`: لا أخطاء ولا تحذيرات. بقيت 74 ملاحظة info فقط، أغلبها أقواس أسلوبية واستخدام `value` المهمل حديثًا في بعض حقول Flutter، ولا تمنع البناء.
- لا توجد credentials في الكود أو السجلات أو الاختبارات.
