# تقرير مراجعة المزامنة قبل تجربة المستخدم

تاريخ التنفيذ: 2026-09-25

## نتيجة الجاهزية

تم استبدال ملاحظة السعر القديمة بعقد Sync v1.1 الذي اعتمد
`salePriceMinor`. أصبح Flutter يدعم الحقل في الإنشاء والتحديث والاستقبال.

## سعر بيع وحدة المنتج

- يرسل Flutter السعر كـinteger غير سالب بوحدة العملة الصغرى.
- يحفظ الجهاز الثاني القيمة نفسها، وتبقى الأحداث التاريخية بلا حقل بقيمة صفر.
- اختبار API الحقيقي ما زال يحتاج credentials ومؤسسة اختبار مأذونًا بها.

## حماية حذف SQLite

الحذف مرفوض مع شرح الأسباب عندما يوجد أي من التالي:

- أي صف غير محسوم في `sync_outbox`، بما فيه pending/failed/conflict/rejected.
- عملية legacy بحالة `pending_review`.
- مسودة محلية في جداول المستندات أو المخزون أو المصروفات والتحويلات.

لا يوجد زر force delete يتجاوز الحماية في واجهة المستخدم.

## التشغيل دون إنترنت

- إذا كانت التوكنات موجودة والسياق المحلي لمؤسسة مفعلة سابقًا صالحًا، يفتح
  التطبيق المؤسسة محليًا عند تعذر `/me` بسبب الشبكة.
- لا يعيد تسجيل الجهاز ولا يختار مؤسسة جديدة في هذا المسار.
- خطأ 401/جلسة منتهية لا يستخدم fallback المحلي؛ يبقى سلوك انتهاء الجلسة قائمًا.

## معنى الحالات في الواجهة

- الحفظ المحلي: العملية محفوظة على هذا الجهاز فقط وقد تبقى في outbox.
- التهيئة المكتملة: bootstrap الجزئي تبعه replay كامل حتى watermark وتثبيت ack.
- المزامنة المكتملة: لا pending ولا conflicts ولا rejected ولا lastError، والجهاز
  غير ملغى.
- المزامنة الحالية **يدوية فقط** من شاشة الإعدادات. لا يوجد background sync أو
  تشغيل تلقائي عند فتح التطبيق/عودة الشبكة، والواجهة تصرح بذلك.

## نتائج الاختبارات المحلية الفعلية

- `flutter test`: 35/35 ناجح.
- يشمل: offline reopen، حماية الحذف، ترحيل v10، انقطاع الشبكة وإعادة نفس
  `eventId`، bootstrap جزئي، جهاز A/B، الفاتورة، الدفع، الإلغاء، وتطابق أرصدة
  المخزون والصندوق والطرف.
- `dart analyze`: لا أخطاء ولا warnings؛ 74 ملاحظة info أسلوبية/واجهات deprecated.

## نتائج API العامة

كل الطلبات التالية أرسلت دون token ودون credentials، وكانت النتيجة المتوقعة:

- `GET /me` → 401.
- `GET /sync/bootstrap` → 401.
- `GET /sync/pull` → 401.
- `GET /sync/status` → 401.

## الاختبار الحقيقي بجهازين

لم ينفذ على مؤسسة إنتاج/اختبار لأن البيئة لا تحتوي credentials اختبار مخصصة،
ولأن عقد السعر غير مكتمل. نتائج جهاز A/B المذكورة أعلاه هي integration tests
تشغل SQLite وSyncEngine وprojector وناقل ذاكرة يحاكي نتائج API، وليست ادعاءً
بنجاح الباك الإنتاجي.

لبدء الاختبار الحقيقي نحتاج من فريق الباك:

1. اعتماد تعديل `salePriceMinor` في Event Catalog وJSON Schema/OpenAPI.
2. حساب اختبار ومؤسسة اختبار فارغة وعضويتين/جهازين مسموحين.
3. ضمان replay الكامل من `after=0` أو bootstrap كامل وفق تقرير متطلبات bootstrap.
4. سياسة تنظيف بيانات الاختبار بعد الانتهاء.
