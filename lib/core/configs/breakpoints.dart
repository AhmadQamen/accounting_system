/// Layout sizing for the desktop shell.
abstract class Breakpoints {
  /// Width of the persistent side navigation sidebar.
  static const double sidebarWidth = 240;

  /// Height of the desktop top bar.
  static const double topBarHeight = 54;

  /// Max width of the content region so cards/text don't sprawl on ultra-wide windows.
  static const double contentMaxWidth = 1320;

  static const double desktop = 600;
}
